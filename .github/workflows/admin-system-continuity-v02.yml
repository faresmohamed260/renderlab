name: Admin System Continuity R&D v0.2

on:
  push:
    branches:
      - rd/phase-28-admin-system-continuity-v02
    paths:
      - '.github/workflows/admin-system-continuity-v02.yml'
      - 'design/rd/admin-system-continuity-v02.md'
      - 'design/prototypes/admin-system-continuity-v02/**'
      - 'scripts/verify-admin-system-continuity-v02.mjs'
  workflow_dispatch:

concurrency:
  group: admin-system-continuity-v02-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify Settings-continuity Admin evidence
        run: node scripts/verify-admin-system-continuity-v02.mjs

      - name: Upload review evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renderlab-admin-system-continuity-v02
          path: artifacts/admin-system-continuity-v02
          if-no-files-found: ignore
          retention-days: 30
