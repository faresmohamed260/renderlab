name: Landing Brand R&D

on:
  pull_request:
    paths:
      - "design/brand/landing-brand-rd-v0.1.md"
      - "design/prototypes/landing-brand-rd-v0.1/**"
      - "scripts/verify-landing-brand-rd.mjs"
      - ".github/workflows/landing-brand-rd.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: renderlab-landing-brand-rd-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  prototype:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package.json
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Verify prototype script syntax
        run: node --check scripts/verify-landing-brand-rd.mjs
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start static prototype
        run: |
          python3 -m http.server 4173 --directory design/prototypes/landing-brand-rd-v0.1 > /tmp/renderlab-landing-rd.log 2>&1 &
          echo $! > /tmp/renderlab-landing-rd.pid
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4173/ > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/renderlab-landing-rd.log
          exit 1
      - name: Verify Landing R&D temporal prototype
        run: node scripts/verify-landing-brand-rd.mjs
      - name: Print static server log on failure
        if: failure()
        run: cat /tmp/renderlab-landing-rd.log || true
      - name: Upload R&D evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renderlab-landing-brand-rd-v0.1
          path: artifacts/landing-brand-rd-v0.1/*.png
          if-no-files-found: warn
          retention-days: 14
