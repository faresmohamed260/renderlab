name: Activity Lifecycle Register R&D v0.1

on:
  push:
    branches:
      - rd/activity-lifecycle-register-v01
    paths:
      - '.github/workflows/activity-lifecycle-register-v01.yml'
      - 'design/rd/activity-lifecycle-register-v01.md'
      - 'design/prototypes/activity-lifecycle-register-v01/**'
      - 'scripts/verify-activity-lifecycle-register-v01.mjs'
  workflow_dispatch:

concurrency:
  group: activity-lifecycle-register-v01-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify Activity concepts and evidence
        run: node scripts/verify-activity-lifecycle-register-v01.mjs

      - name: Upload review evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renderlab-activity-lifecycle-register-v01
          path: artifacts/activity-lifecycle-register-v01
          if-no-files-found: ignore
          retention-days: 30
