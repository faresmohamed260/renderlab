name: Apply Phase 7B Completion Documentation

on:
  push:
    branches:
      - docs/phase-7b-complete
    paths:
      - .github/workflows/apply-phase-7b-complete-docs.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply documentation reconciliation
        run: python .github/scripts/apply-phase-7b-complete-docs.py

      - name: Validate and commit final tree
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/apply-phase-7b-complete-docs.yml
          rm .github/scripts/apply-phase-7b-complete-docs.py
          git diff --check
          git add -A
          git diff --cached --check
          git config user.name "RenderLab Phase 7B Docs Bot"
          git config user.email "121525183+faresmohamed260@users.noreply.github.com"
          git commit -m "Record Phase 7B completion"
          git push
