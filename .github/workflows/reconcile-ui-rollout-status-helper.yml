name: Reconcile UI Rollout Status Helper

on:
  push:
    branches: [docs/reconcile-ui-rollout-status-20260914]
    paths:
      - ".github/workflows/reconcile-ui-rollout-status-helper.yml"

permissions:
  contents: write

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/reconcile-ui-rollout-status-20260914
      - run: python .github/scripts/reconcile-ui-rollout-status.py
      - name: Commit and remove helper
        run: |
          rm .github/workflows/reconcile-ui-rollout-status-helper.yml .github/scripts/reconcile-ui-rollout-status.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "docs: reconcile current UI rollout statuses"
          git push origin HEAD:docs/reconcile-ui-rollout-status-20260914
