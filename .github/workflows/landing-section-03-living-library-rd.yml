name: Landing Section 03 Living Library R&D

on:
  pull_request:
    paths:
      - "design/prototypes/landing-section-03-living-library-rd-v0.1/**"
      - "scripts/verify-landing-section-03-living-library-rd.mjs"
      - ".github/workflows/landing-section-03-living-library-rd.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: renderlab-landing-section-03-living-library-rd-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  prototype:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package.json
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Verify script syntax
        run: node --check scripts/verify-landing-section-03-living-library-rd.mjs
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start isolated static prototype
        run: |
          python3 -m http.server 4173 --directory design/prototypes > /tmp/renderlab-landing-section-03.log 2>&1 &
          echo $! > /tmp/renderlab-landing-section-03.pid
          for i in {1..30}; do
            if curl --fail --silent http://127.0.0.1:4173/landing-section-03-living-library-rd-v0.1/ > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/renderlab-landing-section-03.log
          exit 1
      - name: Verify Living Library section
        run: node scripts/verify-landing-section-03-living-library-rd.mjs
      - name: Print static-server log on failure
        if: failure()
        run: cat /tmp/renderlab-landing-section-03.log || true
      - name: Upload section R&D evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: renderlab-landing-section-03-living-library-rd-v0.1
          path: artifacts/landing-section-03-living-library-rd-v0.1/*.png
          if-no-files-found: warn
          retention-days: 14
