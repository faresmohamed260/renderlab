name: Phase 17 documentation patch

on:
  push:
    branches:
      - plan/phase-17-observability-quality
    paths:
      - .github/workflows/phase17-doc-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout planning branch
        uses: actions/checkout@v4
        with:
          ref: plan/phase-17-observability-quality
          fetch-depth: 0

      - name: Patch authoritative governance docs
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          project_path = Path("PROJECT.md")
          migration_path = Path("docs/ui/UI_MIGRATION.md")
          decisions_path = Path("docs/ui/UI_DECISIONS.md")

          project = project_path.read_text()
          migration = migration_path.read_text()
          decisions = decisions_path.read_text()

          def replace_once(text: str, old: str, new: str, label: str) -> str:
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected one match, found {count}")
              return text.replace(old, new, 1)

          project = replace_once(
              project,
              "**Status: `IN PROGRESS — PHASE 16 COMPLETE / VERIFIED; PHASE 17 CONTRACT EXPANSION NEXT`.**",
              "**Status: `IN PROGRESS — PHASE 16 COMPLETE / MERGED; PHASE 17 CONTRACT ACCEPTED / IMPLEMENTATION NEXT`.**",
              "PROJECT Cycle 3 status",
          )
          project = replace_once(
              project,
              "- **Phase 17 — Observability & Engineering Quality: `ROADMAP`.** Add structured lifecycle/error/capacity visibility and cheaper conventional static/unit verification without weakening the existing configured end-to-end gates.",
              "- **Phase 17 — Observability & Engineering Quality: `CONTRACT ACCEPTED / IMPLEMENTATION NOT STARTED`.** Extend the existing privileged Admin Health and server lifecycle seams with privacy-safe structured diagnostics, truthful lifecycle/failure/failover/capacity/backlog aggregates and conventional cheap lint/typecheck/unit feedback, without weakening configured browser/live-provider gates or requiring a telemetry vendor/event store by default.",
              "PROJECT Phase 17 roadmap line",
          )
          project = replace_once(
              project,
              "Repository PR #99 exact-final-head validation/merge is the remaining closure operation.",
              "PR #99 has since completed exact-final-head validation and merged; authoritative Phase 16 post-merge closure is recorded below.",
              "PROJECT stale Phase 16 merge sentence",
          )

          handoff_start = project.index("## Immediate Handoff\n")
          handoff_end = project.index("\n\n## Phase 14 Implementation Verification", handoff_start)
          new_handoff = """## Immediate Handoff
Phase 16 is fully closed on merged `main`. PR #99 squash-merged exact final head `2352f150e0528f2ba3396afc46ccab80aec4e05e` as `ad3cf2a987b60098fdc361a7f8fc358ae706aeae` after the final 26-workflow affected matrix passed. The merge commit then passed all nine push-triggered workflows recorded in the Phase 16 post-merge closure below. The Phase 17 execution contract is now accepted from a fresh `ad3cf2a...` repository audit; Phase 17 implementation has not started. Continue from that contract only. Do not deploy the application, activate reconciliation/maintenance scheduling, add a telemetry vendor/event store, or begin Phase 18 capability work unless separately justified and authorized."""
          project = project[:handoff_start] + new_handoff + project[handoff_end:]

          if "## Phase 16 Post-Merge Closure — 2026-09-05" in project:
              raise SystemExit("PROJECT Phase 16 post-merge closure already exists")
          if "# Phase 17 Execution Contract — Observability & Engineering Quality" in project:
              raise SystemExit("PROJECT Phase 17 execution contract already exists")

          project += """

## Phase 16 Post-Merge Closure — 2026-09-05
PR #99 squash-merged Phase 16 to `main` as `ad3cf2a987b60098fdc361a7f8fc358ae706aeae` from exact final PR head `2352f150e0528f2ba3396afc46ccab80aec4e05e`.

The final PR head passed the complete 26-workflow affected matrix. Library Lifecycle encountered one transient network `ECONNRESET`/fetch timeout after healthy build/start behavior; exact fixture cleanup completed and the unchanged rerun passed, so no product change was made to manufacture a green result.

Merged-main push validation then passed all nine workflows attached to exact merge SHA `ad3cf2a987b60098fdc361a7f8fc358ae706aeae`: Activity Cancel Visual `33967198561`, Generation Integration `33967198351`, Video Generation Integration `33967198358`, Reference Upload Integration `33967198361`, UI Shell Validation `33967198395`, Generation Reconciliation `33967198451`, Generation Cancellation `33967198513`, Maintenance Integration `33967198402`, and Creative Iteration `33967198317`.

Phase 16 remains `COMPLETE / VERIFIED / MERGED`. Its final rendered evidence remains artifact `9969057974` (`sha256:cc20206371324f0698433731294924105174943cc0176dfd4ce9264fee6e8df5`), with the accepted Result-primary/source-context comparison treatment and no corrective implementation change required.

No production application rollout, reconciliation/maintenance scheduler activation, `pg_cron`/`pg_net` enablement, schema change or provider-routing change followed the Phase 16 merge. Production remains the separately accepted Closed-Beta deployment recorded above. Phase 17 contract expansion is the next repository phase.

---

# Phase 17 Execution Contract — Observability & Engineering Quality
**Status: `CONTRACT ACCEPTED / IMPLEMENTATION NOT STARTED`.**
**UI decision:** UI-057.
**Planning baseline:** merged `main` `ad3cf2a987b60098fdc361a7f8fc358ae706aeae`.

## Goal / user value
Make RenderLab easier to operate and safer to change without turning observability into another product surface or replacing the high-fidelity configured gates that already protect real account/media/generation behavior.

Phase 17 should provide two complementary improvements:
1. engineers get cheap deterministic feedback for conventional code defects before expensive build/browser/live-provider verification; and
2. a fresh-authorized RenderLab admin can understand lifecycle health, failure/failover pressure, capacity and cleanup backlog from bounded privacy-safe aggregates instead of inspecting raw tables/provider traces by hand.

The phase does **not** promise SLAs, provider cost accounting, deterministic render timing or exhaustive distributed tracing.

## Verified starting state — 2026-09-05
1. **Phase 16 is merged and green on `main`.** PR #99 landed as `ad3cf2a...`; all nine merge-triggered workflows succeeded after the final 26-workflow PR matrix.
2. **TypeScript is already strict but not exposed as a cheap script.** `tsconfig.json` has `strict: true` and `noEmit: true`; `package.json` currently has no `typecheck` script.
3. **There is no conventional lint or focused unit-test script.** Current package scripts are development/build/start, `verify:ui-purity` and Playwright UI verification. UI Shell currently runs UI purity, full Next production build, Chromium install/start and Playwright.
4. **Admin Health already exists as the correct operator surface.** Fresh-active-admin bootstrap returns a bounded 24-hour aggregate with `activeJobs`, status counts, operation counts and sanitized failure codes. It does not expose prompts, media contents, provider/worker IDs or raw provider messages.
5. **Lifecycle/maintenance already expose useful structured seams.** `generation_jobs` owns durable status/timestamps/sanitized error/failover state; admission reservations own bounded capacity state; Phase 15 maintenance already returns bounded source/upload/purge summaries. Phase 17 should derive visibility from those contracts before inventing another event store.
6. **Production is intentionally behind repository capability.** Phase 14–16 repository changes are not implicitly deployed and no production reconciliation/maintenance scheduler is active. Phase 17 implementation/merge must preserve that boundary.
7. **No telemetry vendor is currently required by evidence.** Existing platform/server logs plus durable RenderLab aggregates are enough to start. Sentry, OpenTelemetry, Vercel-native additions or another service require an explicit demonstrated gap, privacy review and cost/fit justification rather than adoption for its own sake.

## Required invariants
- **Observability is observational.** Logging/metrics failure must never become a generation, cancellation, maintenance, upload or account correctness dependency.
- **Product truth remains authoritative.** Admin metrics summarize persisted/current product state; they do not fabricate percentages, queue positions, cost, autoscaling capacity or SLA claims.
- **Privacy is deny-by-default.** Diagnostics must not record prompts/negative prompts, media bytes/content/display names, account email, Auth/session tokens or cookies, signed URLs, R2 keys, service/provider credentials, raw request bodies, or raw provider error bodies.
- **Opaque correlation is not authorization.** Correlation/job IDs may connect internal events, but never grant access and never replace owner/fresh-admin checks.
- **Cheap checks supplement high-fidelity gates.** Lint/typecheck/unit feedback must not replace configured Playwright, shared-resource, reconciliation/cancellation/maintenance or live Image/Video integration when those paths are affected.
- **No new operations product.** Ordinary users continue to see product-level state/errors only. Operator observability extends existing fresh-admin Health rather than creating a provider console or new top-level destination.

## 17A — Cheap conventional engineering feedback
Add conventional package-level scripts with these stable purposes:
- `npm run lint` — maintained repository linting for relevant TypeScript/TSX/JavaScript/config sources;
- `npm run typecheck` — TypeScript checking with no emit against the current Next/TS configuration;
- `npm run test:unit` — deterministic focused tests for pure contracts with no network, shared Supabase/R2, browser or provider requirement.

Implementation may choose the lightest maintained Node-compatible lint/test runner after verifying compatibility with the repository's current Next.js/TypeScript/ESM setup. This contract does not require Jest, Vitest or another brand merely for convention.

Focused unit coverage should target pure/high-risk contracts first, expected to include:
- generation request/capability normalization and bounded compatibility helpers;
- recipe/retry reconstruction rules where they can be isolated without service calls;
- sanitized/typed error classification;
- lifecycle timing/backlog aggregation helpers introduced by this phase;
- structured diagnostic event redaction/normalization and correlation behavior.

Add one cheap GitHub engineering-quality gate/workflow so these checks can fail without first installing Chromium or invoking shared cloud/provider fixtures. `verify:ui-purity` remains authoritative for visible primitive policy and may be included in the cheap gate when efficient.

## 17B — Structured server diagnostics and correlation
Introduce one small server-owned diagnostic boundary rather than scattered ad-hoc `console.log` payloads. Events must have a typed/bounded envelope such as:
- event name and level;
- timestamp;
- opaque correlation ID;
- optional opaque RenderLab job ID;
- product operation/lifecycle phase/status where relevant;
- typed diagnostic/error code;
- non-negative duration/count/attempt scalars when meaningful.

Do not add an unrestricted `details` bag that encourages raw request/provider dumping. Any event-specific fields must be explicit bounded scalars/enums and pass the same redaction policy.

Instrument the highest-value lifecycle boundaries rather than every function call, expected to include:
- generation admission/submission accepted or rejected;
- reconciliation claim/outcome, provider-ready/failover/finalization outcome;
- cancellation intent/provider acknowledgement/local terminalization outcome;
- bounded maintenance pass summary and category failures;
- durable result finalization success/failure boundaries where correlation materially helps incident reconstruction.

Correlation IDs are generated/trusted server-side and propagated through internal lifecycle work where useful. A browser-supplied identifier is never trusted for authorization or used to override server correlation state.

## 17C — Privileged Admin Health expansion
Extend the existing `/admin` **Health** section and its fresh-admin server contract; do not add another route or ordinary shell navigation item.

The v0.1 operator view should remain bounded and aggregate. From currently durable state, expose truthful snapshots/rolling-window summaries for the following categories where derivable without scanning unbounded history:
- lifecycle outcomes by status/operation and current active state;
- completion timing aggregates or clearly labelled timing bands from persisted timestamps, with sample counts and no SLA language;
- sanitized failure-code counts;
- failover incidence/counts from persisted failover history without exposing worker/provider identity;
- cancellation/cancelling counts where useful;
- stale-active age buckets aligned with existing lifecycle bounds rather than an invented ETA;
- admission/capacity snapshot from current active jobs/reservations and configured limits, without claiming provider autoscaling capacity;
- maintenance backlog: stale/unreferenced source candidates, claimed/cleaning sources, stale upload candidates, claimed/cleaning uploads and tombstoned pending media purges.

Keep the existing dense/conventional Admin visual language. Prefer compact stat/table composition over a new dashboard framework. If the visible Health composition changes, configured Admin verification must capture desktop + narrow states and human review must confirm readability/no sensitive-data leakage.

The Admin payload must remain aggregate. It must not expose prompts, media content/names, account email/owner identity, R2 identity, provider job IDs, worker IDs, workflow IDs, raw failover payloads or raw provider messages.

## 17D — Typed error classification over text matching
Where current gateway/worker contracts provide stable machine-readable failure categories, normalize them into typed internal diagnostic codes at the adapter boundary and carry only sanitized product codes into user/Admin contracts.

Textual/raw-message classification may remain a compatibility fallback for provider responses that do not yet expose a stable code. Do not break a working gateway or invent a provider code solely to eliminate every string match in one phase.

Any worker/gateway contract change must preserve existing product error privacy and be proven by the affected Image/Video/reconciliation/cancellation regressions. Provider-specific details stay server-internal and are not added to ordinary Activity/Create/Admin payloads.

## 17E — Telemetry/tooling decision boundary
Start Phase 17 with the repository's existing server/platform logging and database-derived bounded aggregates. Evaluate external telemetry only after the structured event/admin-health implementation demonstrates a concrete unanswered need such as retention/search/cross-request correlation that current tooling cannot satisfy reasonably.

Adopting Sentry, OpenTelemetry, a Vercel observability add-on or another vendor requires all of the following before integration:
- specific gap it solves;
- current plan/cost/retention fit;
- server-only vs client collection boundary;
- explicit data/redaction fields;
- secret/configuration ownership;
- failure behavior that cannot block product correctness.

No vendor adoption, client analytics/RUM or persistent event-store schema is a Phase 17 exit requirement.

## Explicitly out of scope
- New top-level Observability/Operations/Workers/Models/Workflows route.
- Ordinary-user provider, worker, queue, failover, cleanup or infrastructure controls.
- Client product analytics, marketing pixels, session replay, prompt/content analytics or behavioral profiling.
- Logging prompts, uploaded/generated media content or names, account email, Auth/session material, signed URLs/R2 keys, credentials or raw provider bodies.
- Billing/credits/provider-cost ledger, GPU utilization claims, exact autoscaling capacity, render-time SLA/ETA or cost promises.
- A generic audit/event-sourcing platform or new durable telemetry table without an audited need and contract amendment.
- Replacing existing Playwright/shared-resource/live-provider gates with unit tests.
- Phase 18 creative capability work, Variations, Director, LoRA/model adapter, Upscale/Restore or Inpaint/Outpaint implementation.
- Production application deployment, production reconciliation/maintenance scheduling, `pg_cron`/`pg_net` activation or unrelated infrastructure mutation.
- UI-054 BIMI/mailbox-avatar implementation or unrelated Auth/email work.

## Architecture / data boundaries
- Existing `generation_jobs`, `generation_admission_reservations`, staging/upload/media tombstone state and Phase 15 maintenance predicates are the first source for operator aggregates.
- Existing Admin fresh-auth/fresh-role boundary remains authoritative. Extend the existing admin contract/service/RPC or use equally bounded server queries; do not grant browsers raw table access.
- A small server observability/diagnostic module is allowed to centralize event normalization/redaction/correlation. It must stay server-only.
- No schema migration is pre-approved. First prove the required v0.1 aggregates from existing timestamps/status/error/failover/reservation/staging fields. If a required metric cannot be derived truthfully and durable new state is genuinely necessary, stop and amend this contract before DDL/event storage.
- Existing RLS/browser-grant revocation and `SECURITY DEFINER` empty-search-path/service-role-only posture must remain intact for any changed privileged function.

## Security / privacy implications
Verification must explicitly prove diagnostic redaction, not merely rely on code review. Tests should seed sentinel prompt/email/storage/provider-error strings and assert they are absent from structured events and Admin responses while expected typed codes/counts remain present.

Logs may use opaque internal job/correlation identity for incident stitching, but account identity should be omitted by default. If implementation proves an owner identifier is required for a specific server-only incident use case, it must be explicitly bounded/hashed or separately justified; it does not enter the Admin aggregate contract.

Do not expose correlation, telemetry or Admin aggregation endpoints as public data-enumeration surfaces. Fresh-admin authorization remains required before privileged health reads.

## Required validation matrix
### Cheap quality gate
- lint passes on exact head and fails deterministically on an injected lint fixture/config test where practical;
- typecheck runs without emit and catches a deliberate type error in verifier/fixture coverage or the selected tool's own regression test;
- unit tests run with no network/secrets/browser and cover the selected pure contracts;
- the cheap gate does not install Chromium or invoke live providers/shared production fixtures.

### Structured diagnostics
- correlation stays stable across one representative submit/reconcile/finalize lifecycle where propagated;
- separate operations receive distinct correlation IDs;
- typed event names/codes/status/durations are bounded and machine-readable;
- diagnostic emission failure is non-fatal to product correctness;
- seeded prompt/email/signed-URL/R2-key/token/raw-provider sentinels never appear in emitted event payloads.

### Admin Health
Use exact run-owned admin/member/job/reservation/staging/media fixtures and prove:
- signed-out/member/non-admin access remains fail-closed;
- exact status/operation/failure/failover/timing/stale/capacity/backlog fixtures produce the expected bounded aggregates;
- historical/user rows outside the run-owned fixtures are not mutated;
- no prohibited identity/content/provider/storage fields appear in the response/rendered page;
- desktop + narrow Admin Health remains readable if the UI composition changes;
- cleanup returns run-owned Auth/Supabase/R2 state and generation singleton defaults to baseline.

### Lifecycle/provider regressions
At minimum audit path filters and run every workflow actually affected, expected to include:
- Engineering Quality / static gate;
- Account/Admin Operations;
- UI Shell Validation when shared package/config/UI paths change;
- Generation Reconciliation;
- Generation Cancellation;
- Maintenance Integration;
- Generation Admission;
- Activity when shared job/error serialization changes;
- Generation Integration and Video Generation Integration when lifecycle adapters/error classification are touched.

Live provider spend is **not** required merely for logging/admin aggregation. Existing configured Image/Video live cases become required when provider adapter/error-classification code is changed, and their exact cleanup remains mandatory.

## Documentation outputs
On verified implementation, update from actual repository state:
- `PROJECT.md` — Phase 17 implementation evidence, exact-head runs and Phase 18 handoff;
- `docs/ui/UI_MIGRATION.md` — checklist, Admin visual review and exact validation/cleanup;
- `docs/ui/UI_DECISIONS.md` — UI-057 implementation evidence or approved amendment;
- `docs/architecture/FRONTEND_ARCHITECTURE.md` — server diagnostic/Admin data boundary if implemented materially;
- `docs/architecture/INFRASTRUCTURE.md` — logging/telemetry/runtime/tooling reality and any privileged-function changes;
- `docs/ui/SCREEN_REGISTRY.md` — actual Admin Health behavior if visible output changes;
- `docs/ui/COMPONENT_CATALOG.md` only if a new approved Admin composition/mechanic is actually introduced;
- `docs/architecture/PRODUCT_CAPABILITIES.md` only if product-visible generation/error capability semantics change.

## Exit criteria
Phase 17 is `COMPLETE / VERIFIED` only when:
- conventional `lint`, `typecheck` and focused `test:unit` scripts provide cheap deterministic feedback and an exact-head GitHub quality gate is green;
- structured server diagnostics/correlation cover the accepted high-value lifecycle boundaries with explicit redaction and non-fatal emission;
- fresh-admin Health exposes truthful bounded lifecycle timing/failure/failover/capacity/stale/backlog visibility without sensitive product/provider/storage data;
- typed error classification improves the adapters that have stable machine-readable evidence while safe compatibility fallbacks remain where needed;
- no telemetry vendor/event-store/schema is introduced without the required evidence/contract amendment;
- affected exact-head configured regressions and any required Admin desktop/narrow human review pass;
- exact run-owned fixtures clean and existing RLS/grant/privileged-function/singleton invariants remain intact;
- authoritative docs match implementation reality;
- no production deployment or scheduler activation is inferred from implementation/merge.

Only after Phase 17 is `COMPLETE / VERIFIED` should Phase 18 be expanded into an execution-ready Next Creative Capability contract from a fresh deployed-worker audit.
"""
          project_path.write_text(project)

          current_start = migration.index("## Current Work\n")
          current_end = migration.index("\n## Session Handoff Rule", current_start)
          new_current = """## Current Work
**Current cycle:** Cycle 3 — Reliability, Creative Iteration & Capability Growth is `IN PROGRESS`; Cycle 2 remains `COMPLETE / VERIFIED`.
**Current phase:** Phase 17 — Observability & Engineering Quality is `CONTRACT ACCEPTED / IMPLEMENTATION NOT STARTED` under UI-057. Phase 16 is `COMPLETE / VERIFIED / MERGED` on `main` `ad3cf2a987b60098fdc361a7f8fc358ae706aeae`.
**Next sequence:** implement Phase 17 only from the accepted execution contract in `PROJECT.md`, beginning with cheap engineering feedback and the existing server/Admin Health seams. Phase 18 remains roadmap-only until Phase 17 is complete and a fresh deployed-worker capability audit expands it.
**Release reality:** exact candidate `d6b8f386db3893e583c99b23fc3397b0eb377d42` remains the accepted Closed-Beta production application at READY deployment `dpl_CZZvmdN42VHRK7uLVUA9W8kdc7x2`. Automatic Git deployment remains disabled.
**Deployment boundary:** Phase 16 merge and Phase 17 planning changed repository state only. They do not authorize or imply a production application rollout, reconciliation/maintenance scheduler activation, `pg_cron`/`pg_net`, telemetry-vendor configuration or production maintenance sweep.
**Broader-beta boundary:** Phase 13 owns production-capable invite/recovery delivery, sender-domain authentication, templates, rate limits and live mailbox evidence. Free-plan leaked-password protection remains separate.
**Next creative direction:** Phase 18 will re-audit deployed workers after Phase 17; current evaluation preference remains Upscale/Restore, then Inpainting/Outpainting, then LoRA/model adapters, with Director still blocked on structured deployed-worker semantics."""
          migration = migration[:current_start] + new_current + migration[current_end:]

          if "## Cycle 3 — Phase 17 Observability & Engineering Quality" in migration:
              raise SystemExit("UI_MIGRATION Phase 17 tracker already exists")
          migration += """

## Cycle 3 — Phase 16 Post-Merge Closure — 2026-09-05
- [x] PR #99 exact final head `2352f150e0528f2ba3396afc46ccab80aec4e05e` completed the 26-workflow affected matrix; the only rerun was an unchanged Library Lifecycle retry after a classified transient `ECONNRESET`/fetch timeout, with cleanup intact.
- [x] Squash-merge PR #99 as `ad3cf2a987b60098fdc361a7f8fc358ae706aeae`.
- [x] Verify all nine merge-triggered push workflows on exact merge SHA: Activity Cancel `33967198561`, Generation `33967198351`, Video Generation `33967198358`, Reference Upload `33967198361`, UI Shell `33967198395`, Reconciliation `33967198451`, Cancellation `33967198513`, Maintenance `33967198402`, Creative Iteration `33967198317`.
- [x] Preserve final Phase 16 rendered approval artifact `9969057974` / `sha256:cc20206371324f0698433731294924105174943cc0176dfd4ce9264fee6e8df5` and the accepted PR #100 comparison hierarchy.
- [x] Keep production rollout/scheduler state unchanged; no Phase 16 merge operation deployed the app or activated reconciliation/maintenance scheduling.

**Phase 16 repository status: `COMPLETE / VERIFIED / MERGED`.**

## Cycle 3 — Phase 17 Observability & Engineering Quality
**Status: `CONTRACT ACCEPTED / IMPLEMENTATION NOT STARTED`.**  
**Decision:** UI-057.  
**Execution contract:** `PROJECT.md` Phase 17.

Verified planning baseline on merged `main` `ad3cf2a987b60098fdc361a7f8fc358ae706aeae`:
- [x] Phase 16 is merged and its nine push-triggered workflows are green.
- [x] Audit current package/TS/CI reality: strict/no-emit TypeScript exists, while conventional `lint`, `typecheck` and focused unit-test scripts do not; UI Shell currently reaches full build/Chromium/Playwright for broad verification.
- [x] Audit existing privileged operator seam: Admin Health is already fresh-admin-only and returns bounded 24-hour active/status/operation/sanitized-failure aggregates.
- [x] Audit lifecycle/maintenance seams: current jobs/admission/failover/timestamps plus Phase 15 bounded maintenance summaries can support the first operator metrics without a speculative event-store migration.
- [x] Lock privacy/no-new-ops-surface boundary under UI-057; ordinary users do not receive provider/worker diagnostics and Admin aggregates do not expose content/identity/storage/provider internals.
- [x] Keep telemetry-vendor adoption evidence-gated rather than a Phase 17 requirement.

Implementation checklist:
- [ ] Add conventional `npm run lint`, `npm run typecheck` and deterministic `npm run test:unit` using the lightest maintained tools compatible with current Next/TypeScript/ESM reality.
- [ ] Add a cheap Engineering Quality GitHub gate that requires no Chromium, live provider or shared cloud fixture.
- [ ] Add one server-owned typed structured-diagnostic/correlation boundary with explicit redaction and non-fatal emission.
- [ ] Instrument high-value admission/submission/reconciliation/finalization/cancellation/maintenance lifecycle boundaries without logging prompt/media/account-secret/raw-provider payloads.
- [ ] Extend fresh-admin Health with bounded truthful timing/failure/failover/stale/capacity/maintenance-backlog aggregates derived from current durable state first.
- [ ] Prefer typed gateway/provider error codes where stable machine-readable contracts exist; retain safe textual compatibility fallback where they do not.
- [ ] Verify redaction with sentinel secrets/content, fresh-admin authorization, exact aggregates, desktop+narrow Admin Health if visible composition changes, and exact run-owned cleanup.
- [ ] Pass every actually affected exact-head regression; keep live Image/Video gates when provider-adapter code is touched rather than replacing them with unit tests.
- [ ] Update architecture/screen/infrastructure docs from implemented reality before Phase 17 completion.
- [ ] Keep schema/event-store/vendor adoption, production deployment and scheduler activation separately evidence-gated/unauthorized by this contract.

**Next after verified completion:** re-audit the deployed worker fleet and expand Phase 18 into one coherent capability contract; do not implement Phase 18 from roadmap preference alone.
"""
          migration_path.write_text(migration)

          if "### UI-057 — Operator observability extends privileged Admin Health" in decisions:
              raise SystemExit("UI_DECISIONS UI-057 already exists")
          decisions += """

### UI-057 — Operator observability extends privileged Admin Health
**Status:** Accepted  
**Date:** 2026-09-05  
**Decision:** Phase 17 operator visibility extends the existing fresh-active-admin `/admin` **Health** section rather than creating a new Observability/Operations destination or adding infrastructure state to ordinary product navigation. The Health contract remains bounded and aggregate: truthful lifecycle status/operation outcomes, labelled timing aggregates/bands, sanitized failure codes, failover/cancellation incidence, stale-active age buckets, admission/capacity snapshot and Phase 15 maintenance backlog may be shown when derivable from current durable state. Structured server diagnostics use opaque server-generated correlation identity and typed bounded event/error fields, but Admin/product payloads do not expose prompts, media contents/names, account identity, R2/storage identity, provider job/worker/workflow IDs or raw provider messages. No client analytics/session replay is introduced by this decision.  
**Reason:** RenderLab already has a privileged Admin Health surface, durable lifecycle timestamps/status/error/failover state, admission reservations and bounded maintenance summaries. Extending those proven seams gives operators useful incident/capacity visibility without fragmenting the product into an infrastructure console or collecting sensitive creative/account data merely for telemetry.  
**Consequences:** Reuse the current fresh-admin authorization and server-owned Admin contract/RPC/query boundary; ordinary Create/Library/Viewer/Activity behavior remains product-level and unchanged. Prefer compact conventional Admin stat/table composition rather than a new dashboard framework. No schema/event-store or telemetry vendor is pre-approved: derive the v0.1 aggregates from existing state first, and amend the Phase 17 contract before adding durable telemetry storage or a vendor that changes privacy/cost/runtime boundaries. Any changed privileged function must preserve RLS/browser-grant revocation, empty `search_path` and service-role-only execution. If Admin Health rendering changes, configured desktop+narrow verification and human review are required. This decision does not authorize production deployment, reconciliation/maintenance scheduling, provider controls, billing/cost/SLA claims, client tracking or Phase 18 capability implementation.
"""
          decisions_path.write_text(decisions)
          PY

      - name: Check documentation diff
        run: |
          git diff --check
          git diff -- PROJECT.md docs/ui/UI_MIGRATION.md docs/ui/UI_DECISIONS.md

      - name: Commit planning docs and remove helper workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add PROJECT.md docs/ui/UI_MIGRATION.md docs/ui/UI_DECISIONS.md
          git rm .github/workflows/phase17-doc-patch.yml
          git commit -m "Plan Phase 17 observability and engineering quality"
          git push origin HEAD:plan/phase-17-observability-quality
