name: Phase 13B Completion Documentation

on:
  push:
    branches: [docs/phase-13b-completion]
    paths:
      - ".github/workflows/phase-13b-completion-docs.yml"

permissions:
  contents: write

jobs:
  update-docs:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/phase-13b-completion
          fetch-depth: 0

      - name: Record verified Phase 13B production state
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          # PROJECT.md
          p=Path('PROJECT.md'); s=p.read_text()
          old="**Cycle 2 — Creative Productivity & Beta Maturity remains `COMPLETE / VERIFIED`. Cycle 3 — Beta Operations & Access Reliability is now `IN PROGRESS`: Phase 13 — Email & Invite Production Hardening is `IN PROGRESS`, with 13A read-only audit `COMPLETE / VERIFIED` and 13B awaiting the explicit provider/sender/credential decision. Production remains the accepted Closed-Beta application `d6b8f386db3893e583c99b23fc3397b0eb377d42` at READY deployment `dpl_CZZvmdN42VHRK7uLVUA9W8kdc7x2`; 13A performed no SMTP, DNS, hosted Auth, Vercel or application mutation.**"
          new="**Cycle 2 — Creative Productivity & Beta Maturity remains `COMPLETE / VERIFIED`. Cycle 3 — Beta Operations & Access Reliability is `IN PROGRESS`: Phase 13 — Email & Invite Production Hardening is `IN PROGRESS`, with 13A read-only audit and 13B sender/delivery configuration both `COMPLETE / VERIFIED`; 13C branded template/link hardening is next. Production application code remains the accepted Closed-Beta candidate `d6b8f386db3893e583c99b23fc3397b0eb377d42` at READY deployment `dpl_CZZvmdN42VHRK7uLVUA9W8kdc7x2`. Phase 13B changed only Brevo/Cloudflare/Supabase Auth email configuration; it created no Vercel deployment and sent no real invite/recovery email.**"
          if old not in s: raise SystemExit('PROJECT current priority anchor missing')
          s=s.replace(old,new,1)
          old="### Cycle 3 — Beta Operations & Access Reliability\n**Status: `PLANNED`.** Cycle 2 remains complete. Cycle 3 begins with one execution-ready phase only; later Cycle 3 work remains undefined until current evidence justifies it.\n\n- **Phase 13 — Email & Invite Production Hardening: `CONTRACTED / NOT STARTED`.** Make RenderLab's already-implemented admin-invite and password-recovery flows dependable for real external Closed-Beta users by replacing Supabase's built-in development-oriented mail posture with a production-capable transactional delivery path, authenticated sender domain, exact token-hash templates, bounded rate limits and live delivery evidence. This contract does not authorize implementation or production mutation by itself."
          new="### Cycle 3 — Beta Operations & Access Reliability\n**Status: `IN PROGRESS`.** Cycle 2 remains complete. Phase 13 is the active Cycle 3 phase; later Cycle 3 work remains undefined until Phase 13 evidence justifies it.\n\n- **Phase 13 — Email & Invite Production Hardening: `IN PROGRESS`.** 13A read-only audit and 13B sender/delivery configuration are `COMPLETE / VERIFIED`. Brevo custom SMTP is now authenticated on `mail.renderlab.faresuniform.uk`, Supabase Auth uses `RenderLab <noreply@mail.renderlab.faresuniform.uk>` at a bounded 30 emails/hour, and 13C exact branded invite/recovery template + link hardening is next. Real mailbox acceptance remains 13D."
          if old not in s: raise SystemExit('PROJECT Cycle3 anchor missing')
          s=s.replace(old,new,1)
          old="### Phase 13 execution contract — Email & Invite Production Hardening\n**Status: `CONTRACTED / NOT STARTED`. Planning/documentation only until the user explicitly authorizes implementation and any required vendor/DNS/Auth production changes.**"
          new="### Phase 13 execution contract — Email & Invite Production Hardening\n**Status: `IN PROGRESS`. 13A and 13B are `COMPLETE / VERIFIED`; 13C is next. The provider/DNS/Auth production changes required by 13B were explicitly authorized and have been applied/verified. This does not authorize unrelated product changes, Vercel deployment or unbounded real-email sending.**"
          if old not in s: raise SystemExit('PROJECT Phase13 status anchor missing')
          s=s.replace(old,new,1)
          old="**13A result:** `COMPLETE / VERIFIED`. **13B sender-domain decision — accepted 2026-08-31:** keep the web application at `renderlab.faresuniform.uk`, isolate RenderLab transactional mail on `mail.renderlab.faresuniform.uk`, and use `RenderLab <noreply@mail.renderlab.faresuniform.uk>` as the default production From identity. This avoids coupling RenderLab mail DNS/reputation to the Vercel web-host CNAME and leaves room for other project-scoped mail subdomains under `faresuniform.uk`. User-facing email design must be professional responsive RenderLab HTML: logo/wordmark, restrained branded header/banner treatment, prominent accessible CTA button, concise security/context copy and a fallback text link; essential content must remain usable when images or advanced CSS are blocked. `faresuniform.uk` does not need to appear as body/footer branding, although the actual sender address remains visible in mail-client sender details. **13B provider decision — accepted 2026-08-31:** Brevo custom SMTP is the approved production transactional-delivery path for RenderLab. **Remaining 13B gate:** create/provision a dedicated RenderLab Brevo SMTP key plus Brevo API key/login in approved GitHub secret storage, then perform provider-specific DNS/Supabase configuration and read-back verification. Do not reuse the existing Brevo Master Password when a dedicated RenderLab SMTP key can be created. No production mail mutation is authorized by this documentation decision alone."
          new="""**13A result:** `COMPLETE / VERIFIED`. **13B sender-domain decision — accepted 2026-08-31:** keep the web application at `renderlab.faresuniform.uk`, isolate RenderLab transactional mail on `mail.renderlab.faresuniform.uk`, and use `RenderLab <noreply@mail.renderlab.faresuniform.uk>` as the default production From identity. User-facing email design remains the accepted professional responsive RenderLab HTML contract under UI-053. **13B provider decision:** Brevo custom SMTP is the approved production transactional-delivery path.

### Phase 13B sender & delivery configuration — COMPLETE / VERIFIED 2026-08-31
- Dedicated `BREVO_API_KEY`, `BREVO_SMTP_LOGIN` and `BREVO_SMTP_KEY` are stored as GitHub repository secrets; Brevo API authentication and SMTP STARTTLS login succeeded without sending a message. The existing Brevo Master Password was not used for the RenderLab configuration.
- Brevo sender domain `mail.renderlab.faresuniform.uk` is provider-verified and authenticated. Final Cloudflare sender DNS contains the two Brevo DKIM CNAME selectors plus Brevo verification TXT at `mail.renderlab.faresuniform.uk` and DMARC TXT at `_dmarc.mail.renderlab.faresuniform.uk`; Brevo reports all four checks passing. Brevo shared-IP authentication did not require a new SPF/MX record, so the existing apex Cloudflare Email Routing MX/SPF posture, Vercel `renderlab.faresuniform.uk` CNAME, Cloudflare Tunnel `auth.faresuniform.uk` and unrelated DNS were preserved.
- A first normalization attempt created two run-owned duplicated TXT names below `mail.renderlab`; corrected run `33399495588` removed exactly those two malformed records, installed the intended TXT names and verified public resolution. Final accepted DNS has no duplicated malformed Phase 13B TXT residue.
- Brevo sender `RenderLab <noreply@mail.renderlab.faresuniform.uk>` is active, and SMTP accepted that envelope sender after authenticated STARTTLS; the verifier issued no recipient/data commands and sent no message.
- Supabase-only run `33399659584` completed successfully after matching the current Management API `smtp_port` string shape. Hosted Auth now reads back `smtp-relay.brevo.com:587`, sender name `RenderLab`, From `noreply@mail.renderlab.faresuniform.uk`, and `rate_limit_email_sent=30`. Production Site URL remains `https://renderlab.faresuniform.uk` and the exact invite/recovery redirect allowlist is preserved.
- Invite/recovery templates remain intentionally unchanged at the end of 13B and are still Phase 13C work. No real Auth invite/recovery email, application-code change, Supabase database migration or Vercel deployment occurred.

**13B result:** `COMPLETE / VERIFIED`. **Next:** 13C installs the accepted branded token-hash invite/recovery templates, verifies link integrity/tracking posture and re-runs deterministic Auth security coverage without live mailbox sends."""
          if old not in s: raise SystemExit('PROJECT 13A result anchor missing')
          p.write_text(s.replace(old,new,1))

          # UI_MIGRATION.md
          p=Path('docs/ui/UI_MIGRATION.md'); s=p.read_text()
          old="Phase 13 is the first active phase of Cycle 3 — Beta Operations & Access Reliability. 13A read-only audit is `COMPLETE / VERIFIED`; the 13B sender-domain/From identity decision is accepted, while transactional provider/account and SMTP credentials remain the explicit operator gate. It remains primarily an Auth/email/infrastructure hardening phase, not a screen redesign. Existing UI-051 Account/Admin and UI-052 Brand/Launch contracts remain authoritative unless live delivery evidence proves a concrete product defect."
          new="Phase 13 is the active phase of Cycle 3 — Beta Operations & Access Reliability. 13A read-only audit and 13B sender/delivery configuration are `COMPLETE / VERIFIED`; 13C branded template/link hardening is next. It remains primarily an Auth/email/infrastructure hardening phase, not a screen redesign. Existing UI-051 Account/Admin, UI-052 Brand/Launch and UI-053 email-identity/design contracts remain authoritative unless verification exposes a concrete product defect."
          if old not in s: raise SystemExit('UI migration phase intro anchor missing')
          s=s.replace(old,new,1)
          old="- [ ] Add/verify only provider-required sender-domain DNS: one valid SPF posture, provider DKIM and explicit DMARC policy/reporting; preserve unrelated Cloudflare DNS records.\n- [ ] Configure Supabase/provider sender/from/reply posture and bounded invitation-only Auth email limits; record actual values without exposing credentials."
          new="- [x] Add/verify only provider-required sender-domain DNS. `mail.renderlab.faresuniform.uk` is Brevo-verified/authenticated with two DKIM CNAMEs, Brevo verification TXT and DMARC TXT; Brevo reports all four checks passing. Brevo shared-IP authentication required no new sender-domain SPF/MX, so apex Cloudflare Email Routing SPF/MX and unrelated Vercel/Tunnel/DNS records were preserved.\n- [x] Configure Supabase/provider sender/from posture and bounded invitation-only Auth email limits. Hosted Auth reads back Brevo relay `smtp-relay.brevo.com:587`, `RenderLab <noreply@mail.renderlab.faresuniform.uk>`, and `rate_limit_email_sent=30`; production Site URL/redirect allowlist remains unchanged."
          if old not in s: raise SystemExit('UI migration 13B checklist anchor missing')
          s=s.replace(old,new,1)
          marker="- The temporary 13A audit workflow was removed after successful evidence capture. Regular CI still does not send real email.\n"
          add="""- The temporary 13A audit workflow was removed after successful evidence capture. Regular CI still does not send real email.

### 13B sender & delivery configuration — COMPLETE / VERIFIED 2026-08-31
- Brevo API + dedicated SMTP credentials were present in GitHub secret storage and authenticated successfully; no credential values were logged or documented and no real email was sent.
- Corrected execution run `33399495588` left exactly the intended sender-domain records: two Brevo DKIM CNAMEs, Brevo verification TXT on `mail.renderlab.faresuniform.uk`, and DMARC TXT on `_dmarc.mail.renderlab.faresuniform.uk`. Brevo reports the domain verified/authenticated and all four DNS checks true. The two malformed duplicated TXT names created by the first normalization attempt were deleted before acceptance.
- `RenderLab <noreply@mail.renderlab.faresuniform.uk>` is an active Brevo sender; authenticated SMTP accepted the envelope sender without any recipient/data send.
- Supabase configuration run `33399659584` passed with exact read-back: custom SMTP `smtp-relay.brevo.com:587`, sender name `RenderLab`, From `noreply@mail.renderlab.faresuniform.uk`, email limit `30/hour`, production Site URL and exact redirects preserved. Templates were deliberately not changed in 13B.
- 13B introduced no application code, database migration, R2/provider-generation change or Vercel deployment. 13C is now the next execution slice.
"""
          if marker not in s: raise SystemExit('UI migration 13A marker missing')
          s=s.replace(marker,add,1)
          old="**Current phase:** Phase 13 — Email & Invite Production Hardening is `IN PROGRESS`; 13A is `COMPLETE / VERIFIED`; Brevo custom SMTP and `RenderLab <noreply@mail.renderlab.faresuniform.uk>` are accepted for 13B, while dedicated Brevo credentials and verified DNS/Supabase configuration remain the execution gate.\n**Next sequence:** create a dedicated RenderLab Brevo SMTP key plus Brevo API key, store `BREVO_SMTP_LOGIN`, `BREVO_SMTP_KEY`, and `BREVO_API_KEY` as GitHub repository secrets, then authenticate `mail.renderlab.faresuniform.uk` with only Brevo-required DNS, configure Supabase custom SMTP + bounded Auth mail limits, and continue to 13C branded invite/recovery templates and link-tracking-safe verification."
          new="**Current phase:** Phase 13 — Email & Invite Production Hardening is `IN PROGRESS`; 13A and 13B are `COMPLETE / VERIFIED`; 13C branded template/link hardening is next.\n**Next sequence:** install/review the professional RenderLab invite and recovery token-hash HTML templates, verify provider/link-tracking posture so Auth URLs are not rewritten, then run deterministic generate-link and negative security coverage without real email. 13D remains the later bounded live-mailbox acceptance slice."
          if old not in s: raise SystemExit('UI migration Current Work anchor missing')
          s=s.replace(old,new,1)
          old="**Deployment boundary:** 13A changed no production configuration. Phase 13 remains configuration-first; any necessary application code fix must create/revalidate an exact candidate before any Vercel rollout."
          new="**Deployment boundary:** 13B changed Brevo/Cloudflare/Supabase Auth mail configuration only; no application code or Vercel deployment changed. Phase 13 remains configuration-first; any necessary application code fix must create/revalidate an exact candidate before any Vercel rollout."
          if old not in s: raise SystemExit('UI migration deployment boundary anchor missing')
          p.write_text(s.replace(old,new,1))

          # INFRASTRUCTURE.md
          p=Path('docs/architecture/INFRASTRUCTURE.md'); s=p.read_text()
          old="**Status: `IN PROGRESS`; 13A `COMPLETE / VERIFIED`, 13B awaiting operator provider/sender/credential decision.** 13A was read-only and did not mutate production."
          new="**Status: `IN PROGRESS`; 13A and 13B `COMPLETE / VERIFIED`, 13C next.** 13A was read-only; 13B applied and verified only Brevo/Cloudflare/Supabase Auth mail configuration and sent no real email."
          if old not in s: raise SystemExit('INFRA Phase13 status anchor missing')
          s=s.replace(old,new,1)
          old="Current Closed-Beta production already has the application-side admission and security contract needed for external invites: admin-only invitation creation/revocation, server-owned `renderlab_beta_invitations`, active account enforcement, fresh Auth-backed private authorization, token-hash invite/recovery confirmation and no public sign-up. Phase 12B corrected hosted Auth Site URL to `https://renderlab.faresuniform.uk` and verified exact invite/recovery redirects. The remaining weakness is mail delivery infrastructure, not account ownership architecture."
          new="Current Closed-Beta production already has the application-side admission and security contract needed for external invites: admin-only invitation creation/revocation, server-owned `renderlab_beta_invitations`, active account enforcement, fresh Auth-backed private authorization, token-hash invite/recovery confirmation and no public sign-up. Phase 12B corrected hosted Auth Site URL to `https://renderlab.faresuniform.uk` and verified exact invite/recovery redirects. Phase 13B has now replaced the built-in mail-delivery posture with verified Brevo custom SMTP; remaining Phase 13 work is branded exact token-hash templates/link integrity plus bounded live mailbox acceptance, not account-ownership architecture."
          if old not in s: raise SystemExit('INFRA remaining weakness anchor missing')
          s=s.replace(old,new,1)
          old="Phase 10D live evidence showed Supabase built-in Auth delivery from `noreply@mail.app.supabase.io` and an `over_email_send_rate_limit` response. That built-in posture remains acceptable for deterministic technical verification only; Phase 13 must replace it for production invite/recovery delivery before broader external-beta reliance."
          new="Phase 10D live evidence showed Supabase built-in Auth delivery from `noreply@mail.app.supabase.io` and an `over_email_send_rate_limit` response. Phase 13B replaced that hosted delivery posture with Brevo custom SMTP; deterministic CI still avoids real email and bounded external mailbox proof remains 13D."
          if old not in s: raise SystemExit('INFRA built-in posture anchor missing')
          s=s.replace(old,new,1)
          marker="Current Brevo documentation confirms transactional SMTP relay (`smtp-relay.brevo.com`, port 587 recommended) and a Free plan including 300 email sends/day. Because domain verification/DKIM/DMARC infrastructure already exists, Brevo custom SMTP is the lowest-drift provider recommendation for Phase 13B. The sender-domain decision is now explicit: the application remains on `renderlab.faresuniform.uk`, while transactional mail is isolated on `mail.renderlab.faresuniform.uk` with default From identity `RenderLab <noreply@mail.renderlab.faresuniform.uk>`. Do not repurpose the existing Vercel-backed `renderlab.faresuniform.uk` CNAME or the Cloudflare-Tunnel-backed `auth.faresuniform.uk` hostname for mail DNS. Brevo custom SMTP is now operator-approved for RenderLab. Dedicated RenderLab Brevo SMTP/API credentials remain the execution gate; the repository does not infer credential availability from DNS alone and must not reuse a general/master SMTP password when a dedicated project key can be provisioned.\n"
          replacement="""Current Brevo documentation confirms transactional SMTP relay (`smtp-relay.brevo.com`, port 587 recommended). The application remains on `renderlab.faresuniform.uk`, while transactional mail is isolated on `mail.renderlab.faresuniform.uk` with default From identity `RenderLab <noreply@mail.renderlab.faresuniform.uk>`. Do not repurpose the existing Vercel-backed `renderlab.faresuniform.uk` CNAME or the Cloudflare-Tunnel-backed `auth.faresuniform.uk` hostname for mail DNS. Brevo custom SMTP is operator-approved and configured; dedicated RenderLab API/SMTP credentials are retained only in approved GitHub secret storage.

#### Phase 13B sender & delivery configuration — verified 2026-08-31
- Execution used temporary branch `work/phase-13b-email-config`; none of its operational workflows are part of `main` and it must not be treated as application release code.
- Brevo API authentication and dedicated SMTP STARTTLS login passed without a message send. Sender domain `mail.renderlab.faresuniform.uk` is both `verified=true` and `authenticated=true`; the exact RenderLab sender `noreply@mail.renderlab.faresuniform.uk` is active and accepted as SMTP MAIL FROM without RCPT/DATA.
- Final provider-required Cloudflare DNS is isolated to the sending subdomain: `brevo1._domainkey.mail.renderlab.faresuniform.uk` CNAME, `brevo2._domainkey.mail.renderlab.faresuniform.uk` CNAME, `mail.renderlab.faresuniform.uk` Brevo verification TXT, and `_dmarc.mail.renderlab.faresuniform.uk` DMARC TXT. Brevo reports all four records passing. Brevo shared-IP authentication did not require a sender-domain SPF or MX record; the existing apex Cloudflare Email Routing MX/SPF, Vercel web CNAME, Tunnel host and unrelated DNS remain unchanged.
- Corrected run `33399495588` removed the two malformed duplicated TXT owner names created by the first normalization attempt before accepting the final DNS state; no malformed Phase 13B TXT residue remains.
- Supabase Management API run `33399659584` returned HTTP 200 and exact read-back for custom SMTP host `smtp-relay.brevo.com`, port `587`, From `noreply@mail.renderlab.faresuniform.uk`, sender name `RenderLab`, and `rate_limit_email_sent=30`. The current API expects `smtp_port` in its string shape. Site URL remains `https://renderlab.faresuniform.uk`; exact invite/recovery redirects remain preserved.
- 13B deliberately did not change invite/recovery template bodies, send a real Auth message, change application code/schema/R2/generation infrastructure or create a Vercel deployment. Template/link hardening is 13C; real external delivery acceptance is 13D.
"""
          if marker not in s: raise SystemExit('INFRA Brevo marker missing')
          p.write_text(s.replace(marker,replacement,1))
          PY

          git diff --check
          git rm .github/workflows/phase-13b-completion-docs.yml
          git add PROJECT.md docs/ui/UI_MIGRATION.md docs/architecture/INFRASTRUCTURE.md
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: complete Phase 13B email configuration"
          git push origin HEAD:docs/phase-13b-completion
