# RenderLab Complete Landing R&D v0.1

**Status:** EXPERIMENTAL COMPLETE-SURFACE ASSEMBLY  
**Source sections:** approved Hero A / Lab Matrix v0.3, Section 02 One creative thread, Section 03 Living Library, Section 04 Resolve to Create  
**Production boundary:** design evidence only; production `/` remains untouched

## Purpose
This prototype assembles the four individually directionally approved Landing sections into one continuous page so the full experience can be judged for pacing, continuity, repetition, responsive flow and final conversion before any production implementation is authorized.

## Assembly rules
The accepted section contracts remain authoritative. This pass may only make bounded connective adjustments needed for one-page continuity, including spacing, handoff pacing, repeated labels, navigation persistence and mobile compaction. It must not reinterpret the approved Lab Matrix identity or invent new marketing claims.

## Full-page story
1. **Lab Matrix hero** — concise product thesis beside the four media cells derived from the locked modular `R`.
2. **One creative thread** — the same representative work progresses through Create → References → Motion → Library on a sticky media stage.
3. **Living Library** — durable media becomes a spatial field; focusing one item causes surrounding work to yield while truthful continuation context updates.
4. **Resolve to Create** — active Library media converges back into the modular `R` geometry before the truthful closed-beta `Open Create` / `Sign in` close.
5. **Minimal footer** — quiet brand/product/access continuity only; no extra marketing block.

## Responsive intent
- Desktop preserves the approved scroll/pointer/spatial mechanics.
- 390px uses the approved compact hero, sticky creative-thread treatment, touch-first horizontal Library rail and already-resolved closing `R`.
- Reduced motion keeps all meaning and settles temporal sections to complete static states.

## Product truth
- `Open Create` → `/create`
- `Sign in` → `/settings`
- closed beta / invitation only / no public sign-up remains explicit
- no pricing, testimonials, fake metrics, provider/model claims, public admission, automatic variants or lineage claims

## Media
The prototype continues to use deterministic external R&D photography. These are not production assets. Final production media must be owned/cleared or RenderLab-generated while preserving the approved density, crops, continuity and motion intent.

## Review gate
Before user review, verify:
- 1440×900 hero and full-page composition;
- Section 02 scroll states and reverse-safe state calculation;
- Section 03 pointer/keyboard selection and spatial yield;
- Section 04 carry-over → resolved progression and final route targets;
- 390×844 full-page flow plus Section 02 / Library / close viewport states;
- reduced-motion static completeness;
- exact locked mark loading;
- all representative media loaded;
- no document-level horizontal overflow;
- no runtime console/page errors.

Complete-page approval is still a design gate only. Production implementation remains a separately scoped, validated operation and deployment remains separately authorized.
